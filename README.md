# WebAuthn reverse proxy
Minimal WebAuthn HTTP reverse proxy which acts as a WebAuthn client and translates CBOR encoded data sent by a FIDO2 authenticator to the JSON format expected by a FIDO2 server.  
